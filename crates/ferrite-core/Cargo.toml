[package]
name = "ferrite-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
crash-testing = []

[dependencies]
fs2 = "0.4"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
